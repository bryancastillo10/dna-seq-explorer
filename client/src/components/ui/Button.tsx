import styled from "styled-components";

const Button = () => {
  return (
      <ButtonContainer>
        Button
      </ButtonContainer>
  )
}

export default Button;

const ButtonContainer = styled.button`
    padding: 14px 20px;
`
